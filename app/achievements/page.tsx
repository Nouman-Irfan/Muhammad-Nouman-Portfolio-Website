import type { Metadata } from "next";import { PageHeader } from "../components/SiteChrome";import { achievements } from "../data/content";
export const metadata:Metadata={title:"Achievements",description:"Leadership, teamwork, and university event experience."};
export default function Achievements(){return <><PageHeader eyebrow="Beyond the code" title="Leadership through participation." copy="University activities that developed my communication, coordination, operations, and teamwork."/><section className="section"><div className="wrap achievement-list">{achievements.map((a,i)=><article key={a.name}><div className="achievement-index">0{i+1}</div><div className="achievement-copy"><span className="eyebrow">{a.date}</span><h2>{a.name}</h2><h3>{a.role}</h3><p>{a.desc}</p><div className="tags">{a.skills.map(s=><span key={s}>{s}</span>)}</div></div>{a.pdf?<a className="achievement-media has-image" href={a.pdf} target="_blank" rel="noreferrer" aria-label={`View ${a.name} certificate`}><img src={a.image} alt={`${a.name} certificate`}/><span>View certificate ↗</span></a>:<div className="achievement-media has-image"><img src={a.image} alt={`${a.name} activity`}/><span>Event highlight</span></div>}</article>)}</div></section></>}
