"use client";import { useState } from "react";import { additionalProjects,featuredProjects } from "../data/content";
const filters=["All","C++","Java","Python","Game Development","Console Applications"];
export function ProjectExplorer(){const [filter,setFilter]=useState("All");const [flipped,setFlipped]=useState<string|null>(null);const featured=featuredProjects.filter(p=>filter==="All"||p.tech.includes(filter)||p.category===filter);const extra=additionalProjects.filter(p=>filter==="All"||p.language===filter||p.category===filter);return <><div className="filters" role="group" aria-label="Filter projects">{filters.map(f=><button key={f} className={filter===f?"active":""} onClick={()=>setFilter(f)}>{f}</button>)}</div><div className="project-grid">{featured.map(p=><article key={p.title} className={`flip-card ${flipped===p.title?"flipped":""}`}><div className="flip-inner"><button className="flip-front" aria-label={`View details for ${p.title}`} onClick={()=>setFlipped(p.title)}><div className="project-art"><span>{p.mark}</span><i>{p.tech.join(" / ")}</i></div><div className="project-body"><small>{p.category}</small><h2>{p.title}</h2><div className="tags">{p.tech.map(t=><span key={t}>{t}</span>)}</div><b>View project details →</b></div></button><div className="flip-back"><button className="flip-close" onClick={()=>setFlipped(null)} aria-label="Close project details">×</button><small>{p.category}</small><h2>{p.title}</h2><p>{p.desc}</p><ul>{p.features.map(f=><li key={f}>{f}</li>)}</ul><a className="button light" href={p.repo} target="_blank" rel="noreferrer">GitHub repository ↗</a></div></div></article>)}</div>{extra.length>0&&<><div className="section-title compact"><div><span className="eyebrow">More experiments</span><h2>Additional projects</h2></div></div><div className="repo-list">{extra.map(p=><a key={p.title} href={p.repo} target="_blank" rel="noreferrer"><div><small>{p.category}</small><h3>{p.title}</h3></div><span>{p.language}</span><b>↗</b></a>)}</div></>}</>}
